<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.board.mapper.UploadMapper"> 
    <insert id="upload" parameterType="kr.board.domain.Tbl_upload">
        insert into tbl_upload(upload_file, upload_date, member_id, upload_memo)
        values(#{upload_file}, now(), #{member_id}, #{upload_memo})
    </insert>
</mapper>